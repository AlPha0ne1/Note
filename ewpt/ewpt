Which version of HTTP introduced support for persistent connections, reducing the overhead of establishing a new connection for each request? > HTTP/1.1

In an HTTP request, which part specifies the resource being requested from the server? > Request URL

The PATCH method - it is used to apply partial modifications to a resource. It is similar to the PUT method but only updates specific parts of the
resource rather than replacing the entire resource.
The HEAD method  - it is similar to the GET method but only retrieves the response headers and not the response body. It is often used to check
the headers for things like resource existence or modification dates.

In HTTP response parsing, what does the "Content-Type" header specify? > Type of data in the response body

Which HTTP response header informs the browser how long it should cache the response? > Cache-Control

What does the "Referer" header in an HTTP request typically contain? > The URL of the previous web page from which the request originated.

Response header
---------------
DAV: 1,2 

Web DAV is a protocol, that allows users on the web to share copy,move and edit files. DAV 1,2 means that suppors DAV version 1 & 2. That response header is typically seen 
in upload directory.

Upload file with the curl
-------------------------
#curl http://192.157.170.3/uploads/ --upload-file simple-backdoor.php
________________________________________________________________________________________________________________________________________________________________________________________________

How does following a penetration testing methodology benefit an organization? > It ensures that the testing process is consistent and repeatable across different applications.

Why is it important to use a structured web application penetration testing methodology? > It provides a systematic approach to identifying and addressing security vulnerabilities.
________________________________________________________________________________________________________________________________________________________________________________________________
Which of the following OWASP Top 10 vulnerabilities refers to the act of an attacker injecting malicious code into a web application, which is then executed by unsuspecting users? > XSS

What is the primary concern related to the OWASP Top 10 vulnerability "Security Misconfiguration"? > Unauthorized access to sensitive files and directories
_________________________________________________________________________________________________________________________________________________________________________________________________
Which of the following is NOT a typical activity performed during the pre-engagement phase of a web application penetration test? > Requesting the source code of the web application for a thorough analysis.

Which section of the penetration test report focuses on providing a technical breakdown of each identified vulnerability? > Vulnerability Details

Why is it essential to include the potential impact and risks associated with the identified vulnerabilities in the report? > To help the organization prioritize remediation efforts based on the risks posed.
_________________________________________________________________________________________________________________________________________________________________________________________________
Does HTTRack utilize spidering to find and download files from a webserver? > False

In addition to taking website screenshots, EyeWitness also downloads the website source code. > True

How many information gathering tests are outlined in the OWASP Web Security Testing Guide? > 10
_________________________________________________________________________________________________________________________________________________________________________________________________
Proxy servers allow you to intercept and modify requests sent between a browser and web application. > False (it is browser and server)

Which one of the following Burp Suite community edition modules has throttling constraints? > intruder

What edition of Burp Suite provides you with scalable scanning options? > Burp Suite Enterprise

Which one of the following system resources is not taken into account when calculating the estimated system impact of a Burp Suite extension? > Disk Space
_________________________________________________________________________________________________________________________________________________________________________________________________
